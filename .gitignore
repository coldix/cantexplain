node_modules/
.DS_Store
.env
.env.*
dist/
.astro/
.wrangler/

# Copied from /evidence on every dev/build. Source of truth stays at repo root
# so Git commit history is the immutable timestamp.
public/evidence/

# Regenerated every build (git SHA + Melbourne stamp)
src/generated/build-meta.json
public/build-meta.json
